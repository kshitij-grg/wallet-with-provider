import 'package:flutter/material.dart';

class RegisterNumberBody extends StatelessWidget {
  const RegisterNumberBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
